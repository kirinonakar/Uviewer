<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Uviewer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Uviewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:uc="using:Uviewer.Controls"
    mc:Ignorable="d"
    Title="Uviewer - Image &amp; Text Viewer">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <ContentControl x:Name="RootFontControl" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">
        <Grid x:Name="RootGrid" AllowDrop="True" IsTabStop="True" Background="Transparent"
              DragOver="Grid_DragOver" Drop="Grid_Drop"
              PointerMoved="RootGrid_PointerMoved"
              PointerExited="RootGrid_PointerExited"
              PointerPressed="RootGrid_PointerPressed">
        <Grid.Resources>
            <ResourceDictionary>
                <ResourceDictionary.ThemeDictionaries>
                    <ResourceDictionary x:Key="Light">
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#15000000" />
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#25000000" />
                        <SolidColorBrush x:Key="ToggleButtonBackgroundPointerOver" Color="#15000000" />
                        <SolidColorBrush x:Key="ToggleButtonBackgroundPressed" Color="#25000000" />
                    </ResourceDictionary>
                    <ResourceDictionary x:Key="Dark">
                        <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#15FFFFFF" />
                        <SolidColorBrush x:Key="ButtonBackgroundPressed" Color="#25FFFFFF" />
                        <SolidColorBrush x:Key="ToggleButtonBackgroundPointerOver" Color="#15FFFFFF" />
                        <SolidColorBrush x:Key="ToggleButtonBackgroundPressed" Color="#25FFFFFF" />
                    </ResourceDictionary>
                </ResourceDictionary.ThemeDictionaries>

                <Style x:Key="ToolbarButtonStyle" TargetType="Button">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="4" />
                    <Setter Property="CornerRadius" Value="4" />
                    <Setter Property="MinWidth" Value="32" />
                    <Setter Property="MinHeight" Value="32" />
                </Style>

                <Style x:Key="ToolbarToggleButtonStyle" TargetType="ToggleButton">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Padding" Value="4" />
                    <Setter Property="CornerRadius" Value="4" />
                    <Setter Property="MinWidth" Value="32" />
                    <Setter Property="MinHeight" Value="32" />
                </Style>

                <DataTemplate x:Key="TextItemTemplate">
                    <TextBlock TextWrapping="Wrap" HorizontalAlignment="Left" />
                </DataTemplate>
                <DataTemplate x:Key="AozoraItemTemplate">
                    <RichTextBlock IsTextSelectionEnabled="False" TextWrapping="Wrap" HorizontalAlignment="Left" Margin="0,0,0,10" />
                </DataTemplate>
            </ResourceDictionary>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="32" Background="{ThemeResource LayerFillColorDefaultBrush}" IsTabStop="False">
            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center" Margin="12,0,0,0">
                <FontIcon Glyph="&#xE8B9;" FontSize="16" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                <TextBlock Text="Uviewer" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
        </Grid>

        <!-- Title Bar & Toolbar -->
        <Grid x:Name="ToolbarGrid" Grid.Row="1" Padding="12,8" 
              Background="{ThemeResource LayerFillColorDefaultBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1"
              IsTabStop="False">
            <Grid.Resources>
                <Style TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}" />
                <Style TargetType="ToggleButton" BasedOn="{StaticResource ToolbarToggleButtonStyle}" />
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="0" VerticalAlignment="Center">
                <Button x:Name="SettingsButton" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,0,0" ToolTipService.ToolTip="설정">
                    <FontIcon Glyph="&#xE713;" FontSize="18"/>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem x:Name="ChangeFontMenuItem" Text="폰트 변경" Click="FontMenu_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8D2;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSubItem x:Name="EncodingMenuItem" Text="인코딩 (Encoding)">
                                <MenuFlyoutSubItem.Icon>
                                    <FontIcon Glyph="&#xE164;"/>
                                </MenuFlyoutSubItem.Icon>
                                <ToggleMenuFlyoutItem x:Name="EncAutoItem" Text="자동 (Detect)" IsChecked="True" Click="EncodingItem_Click" Tag="Auto"/>
                                <ToggleMenuFlyoutItem x:Name="EncUtf8Item" Text="UTF-8" Click="EncodingItem_Click" Tag="UTF-8"/>
                                <ToggleMenuFlyoutItem x:Name="EncEucKrItem" Text="Korean (EUC-KR)" Click="EncodingItem_Click" Tag="EUC-KR"/>
                                <ToggleMenuFlyoutItem x:Name="EncSjisItem" Text="Japanese (Shift-JIS)" Click="EncodingItem_Click" Tag="Shift-JIS"/>
                                <ToggleMenuFlyoutItem x:Name="EncJohabItem" Text="Korean (Johab)" Click="EncodingItem_Click" Tag="Johab"/>
                            </MenuFlyoutSubItem>
                            <MenuFlyoutItem x:Name="ChangeColorsMenuItem" Text="배경색/글자색" Click="ColorsMenu_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE790;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem x:Name="ChangeUiFontMenuItem" Text="UI 폰트 변경" Click="UiFontMenu_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE18F;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSubItem x:Name="LanguageMenuItem" Text="언어 (Language)">
                                <MenuFlyoutSubItem.Icon>
                                    <FontIcon Glyph="&#xE909;"/>
                                </MenuFlyoutSubItem.Icon>
                                <ToggleMenuFlyoutItem x:Name="LangAutoItem" Text="자동 (Default)" Click="LanguageItem_Click" Tag="Auto"/>
                                <ToggleMenuFlyoutItem x:Name="LangKoItem" Text="한국어" Click="LanguageItem_Click" Tag="ko-KR"/>
                                <ToggleMenuFlyoutItem x:Name="LangEnItem" Text="English" Click="LanguageItem_Click" Tag="en-US"/>
                                <ToggleMenuFlyoutItem x:Name="LangJaItem" Text="日本語" Click="LanguageItem_Click" Tag="ja-JP"/>
                            </MenuFlyoutSubItem>
                            <MenuFlyoutSeparator/>
                            <ToggleMenuFlyoutItem x:Name="MatchControlDirectionMenuItem" Text="컨트롤 방향 일치" Click="MatchControlDirectionMenuItem_Click">
                                <ToggleMenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8AB;"/>
                                </ToggleMenuFlyoutItem.Icon>
                            </ToggleMenuFlyoutItem>
                            <ToggleMenuFlyoutItem x:Name="AllowMultipleInstancesMenuItem" Text="다중 실행" Click="AllowMultipleInstancesMenuItem_Click">
                                <ToggleMenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE8A9;"/>
                                </ToggleMenuFlyoutItem.Icon>
                            </ToggleMenuFlyoutItem>
                            <ToggleMenuFlyoutItem x:Name="AutoDoublePageForArchiveMenuItem" Text="압축파일 자동 2장보기" Click="AutoDoublePageForArchiveMenuItem_Click">
                                <ToggleMenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE11E;"/>
                                </ToggleMenuFlyoutItem.Icon>
                            </ToggleMenuFlyoutItem>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem x:Name="AboutMenuItem" Text="정보 (About)" Click="AboutMenu_Click">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE946;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <ToggleButton x:Name="GlobalThemeToggleButton" Click="GlobalThemeToggleButton_Click" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="4,0,0,0">
                    <FontIcon x:Name="ThemeIcon" Glyph="&#xE708;" FontSize="18"/>
                </ToggleButton>
                <ToggleButton x:Name="PinButton" Click="PinButton_Click" IsChecked="True" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="4,0,0,0"
                              ToolTipService.ToolTip="UI 고정/자동숨김 (`)">
                    <FontIcon x:Name="PinIcon" Glyph="&#xE890;" FontSize="18"/>
                </ToggleButton>
                <ToggleButton x:Name="AlwaysOnTopButton" Click="AlwaysOnTopButton_Click" Style="{StaticResource ToolbarToggleButtonStyle}" Margin="4,0,0,0"
                              ToolTipService.ToolTip="항상 위 (T)">
                    <FontIcon x:Name="AlwaysOnTopIcon" Glyph="&#xE718;" FontSize="18"/>
                </ToggleButton>
            </StackPanel>

            <!-- Toolbar -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                <Button x:Name="ToggleSidebarButton" Click="ToggleSidebarButton_Click" ToolTipService.ToolTip="사이드바 토글(Ctrl+B)">
                    <FontIcon Glyph="&#xE8A0;" FontSize="14"/>
                </Button>

                <Button x:Name="FavoritesButton" Click="FavoritesButton_Click" ToolTipService.ToolTip="즐겨찾기">
                    <FontIcon Glyph="&#xE734;" FontSize="14"/>
                    <Button.Flyout>
                        <Flyout x:Name="FavoritesFlyout" Placement="Bottom">
                            <StackPanel Width="400" MaxHeight="500">
                                <Button x:Name="AddToFavoritesButton" Content="➕ 즐겨찾기 추가" Click="AddToFavoritesMenuItem_Click" 
                                        HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                        Background="Transparent" BorderThickness="0"/>
                                <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,4"/>
                                <Pivot x:Name="FavoritesPivot">
                                    <Pivot.HeaderTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" FontSize="14" FontWeight="SemiBold"/>
                                        </DataTemplate>
                                    </Pivot.HeaderTemplate>
                                    <PivotItem x:Name="FileFavoritesPivotItem" Header="{x:Bind local:Strings.FavoritesFiles}">
                                        <ScrollViewer MaxHeight="400" 
                                                     HorizontalScrollBarVisibility="Disabled" 
                                                     VerticalScrollBarVisibility="Auto">
                                            <uc:BookmarkListControl x:Name="FileFavoritesList" 
                                                                    ItemClicked="BookmarkList_ItemClicked"
                                                                    RemoveClicked="BookmarkList_RemoveClicked"
                                                                    PinClicked="BookmarkList_PinClicked"/>
                                        </ScrollViewer>
                                    </PivotItem>
                                    <PivotItem x:Name="FolderFavoritesPivotItem" Header="{x:Bind local:Strings.FavoritesFolders}">
                                        <ScrollViewer MaxHeight="400" 
                                                     HorizontalScrollBarVisibility="Disabled" 
                                                     VerticalScrollBarVisibility="Auto">
                                            <uc:BookmarkListControl x:Name="FolderFavoritesList" 
                                                                    ItemClicked="BookmarkList_ItemClicked"
                                                                    RemoveClicked="BookmarkList_RemoveClicked"
                                                                    PinClicked="BookmarkList_PinClicked"/>
                                        </ScrollViewer>
                                    </PivotItem>
                                </Pivot>
                            </StackPanel>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <Button x:Name="RecentButton" Click="RecentButton_Click" ToolTipService.ToolTip="최근 이미지">
                    <FontIcon Glyph="&#xE823;" FontSize="14"/>
                    <Button.Flyout>
                        <Flyout x:Name="RecentFlyout" Placement="Bottom">
                            <ScrollViewer MaxHeight="600" Width="400" 
                                         HorizontalScrollBarVisibility="Disabled" 
                                         VerticalScrollBarVisibility="Auto">
                                    <uc:BookmarkListControl x:Name="RecentList" 
                                                            ItemClicked="RecentList_ItemClicked"
                                                            RemoveClicked="RecentList_RemoveClicked"/>
                            </ScrollViewer>
                        </Flyout>
                    </Button.Flyout>
                </Button>

                <AppBarSeparator/>

                <Button x:Name="OpenFileButton" Click="OpenFileButton_Click" ToolTipService.ToolTip="파일 열기(Ctrl+O)">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8E5;" FontSize="14"/>
                    </StackPanel>
                </Button>

                <Button x:Name="OpenFolderButton" Click="OpenFolderButton_Click" ToolTipService.ToolTip="폴더 열기">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xED25;" FontSize="14"/>
                    </StackPanel>
                </Button>


                <AppBarSeparator/>

                <!-- Image Toolbar -->
                <StackPanel x:Name="ImageToolbarPanel" Orientation="Horizontal" Spacing="4" Visibility="Visible">
                    <!-- PDF specific buttons moved to front to match text mode -->
                    <Button x:Name="PdfTocButton" Click="PdfTocButton_Click" Visibility="Collapsed" ToolTipService.ToolTip="목차(T)">
                        <FontIcon Glyph="&#xE8FD;" FontSize="14"/>
                        <Button.Flyout>
                            <Flyout x:Name="PdfTocFlyout" Placement="Bottom">
                                <Grid Width="300" Height="400">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="목차" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                    <ListView x:Name="PdfTocListView" Grid.Row="1" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="PdfTocListView_ItemClick">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding HeadingText}" Margin="{Binding Margin}" FontSize="14" 
                                                           FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                                           ToolTipService.ToolTip="{Binding HeadingText}" TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Flyout>
                        </Button.Flyout>
                    </Button>

                    <Button x:Name="PdfGoToPageButton" Click="GoToPageButton_Click" Visibility="Collapsed" ToolTipService.ToolTip="페이지 이동(G)">
                        <TextBlock Text="G" FontSize="14" FontWeight="Bold"/>
                    </Button>

                    <AppBarSeparator x:Name="PdfSeparator" Visibility="Collapsed"/>

                    <Button x:Name="ZoomOutButton" Click="ZoomOutButton_Click" ToolTipService.ToolTip="축소(-)">
                        <FontIcon Glyph="&#xE71F;" FontSize="14"/>
                    </Button>

                    <TextBlock x:Name="ZoomLevelText" Text="100%" Width="50" TextAlignment="Center" 
                               VerticalAlignment="Center" FontSize="12"/>

                    <Button x:Name="ZoomInButton" Click="ZoomInButton_Click" ToolTipService.ToolTip="확대(+)">
                        <FontIcon Glyph="&#xE8A3;" FontSize="14"/>
                    </Button>

                    <Button x:Name="ZoomFitButton" Click="ZoomFitButton_Click" ToolTipService.ToolTip="맞춤">
                        <FontIcon Glyph="&#xE9A6;" FontSize="14"/>
                    </Button>

                    <Button x:Name="ZoomActualButton" Click="ZoomActualButton_Click" ToolTipService.ToolTip="원본 크기">
                        <TextBlock Text="1:1" FontSize="12" FontWeight="SemiBold"/>
                    </Button>

                </StackPanel>

                <!-- Text Toolbar -->
                <StackPanel x:Name="TextToolbarPanel" Orientation="Horizontal" Spacing="4" Visibility="Collapsed">
                    <ToggleButton x:Name="AozoraToggleButton" Click="AozoraToggleButton_Click" ToolTipService.ToolTip="Aozora/md(A)">
                        <TextBlock Text="A" FontSize="14" FontWeight="Bold"/>
                    </ToggleButton>
                    
                    <ToggleButton x:Name="VerticalToggleButton" Click="VerticalToggleButton_Click">
                        <TextBlock Text="V" FontSize="14" FontWeight="Bold"/>
                    </ToggleButton>
                    
                    <Button x:Name="FontToggleButton" Click="FontToggleButton_Click" ToolTipService.ToolTip="폰트 변경(F)">
                        <TextBlock Text="F" FontSize="14" FontWeight="Bold"/>
                    </Button>
                    
                    <Button x:Name="TocButton" Click="TocButton_Click" ToolTipService.ToolTip="목차(T)">
                        <FontIcon Glyph="&#xE8FD;" FontSize="14"/>
                        <Button.Flyout>
                            <Flyout x:Name="TocFlyout" Placement="Bottom">
                                <Grid Width="300" Height="400">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="목차" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                                    <ListView x:Name="TocListView" Grid.Row="1" SelectionMode="Single" IsItemClickEnabled="True" ItemClick="TocListView_ItemClick">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding HeadingText}" Margin="{Binding Margin}" FontSize="14" 
                                                           FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                                           ToolTipService.ToolTip="{Binding HeadingText}" TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Flyout>
                        </Button.Flyout>
                    </Button>

                    <Button x:Name="GoToPageButton" Click="GoToPageButton_Click" ToolTipService.ToolTip="페이지 이동(G)">
                        <TextBlock Text="G" FontSize="14" FontWeight="Bold"/>
                    </Button>

                    <AppBarSeparator/>

                    <Button x:Name="TextSizeDownButton" Click="TextSizeDownButton_Click" ToolTipService.ToolTip="글자 작게(-)">
                        <FontIcon Glyph="&#xE71F;" FontSize="14"/>
                    </Button>

                    <TextBlock x:Name="TextSizeLevelText" Text="18" Width="30" TextAlignment="Center" 
                               VerticalAlignment="Center" FontSize="12"/>

                    <Button x:Name="TextSizeUpButton" Click="TextSizeUpButton_Click" ToolTipService.ToolTip="글자 크게(+)">
                        <FontIcon Glyph="&#xE8A3;" FontSize="14"/>
                    </Button>

                    <AppBarSeparator/>

                    <Button x:Name="ThemeToggleButton" Click="ThemeToggleButton_Click" ToolTipService.ToolTip="배경색 변경(B)">
                        <FontIcon Glyph="&#xE790;" FontSize="14"/>
                    </Button>
                </StackPanel>

                <StackPanel x:Name="SideBySideToolbarPanel" Orientation="Horizontal" Spacing="4" Visibility="Visible">
                    <AppBarSeparator/>

                    <Button x:Name="SideBySideButton" Click="SideBySideButton_Click" ToolTipService.ToolTip="좌우 보기(Space)">
                        <TextBlock x:Name="SideBySideText" Text="1" FontSize="14" FontWeight="SemiBold" Width="14" TextAlignment="Center"/>
                    </Button>

                    <Button x:Name="NextImageSideButton" Click="NextImageSideButton_Click" ToolTipService.ToolTip="다음 그림 위치">
                        <FontIcon x:Name="NextImageSideText" Glyph="&#xE111;" FontSize="14"/>
                    </Button>
                </StackPanel>

                <AppBarSeparator x:Name="SharpenSeparator"/>

                <ToggleButton x:Name="SharpenButton" Width="36" Height="32" Click="SharpenButton_Click" ToolTipService.ToolTip="{x:Bind local:Strings.SharpenTooltip}">
                    <ToggleButton.ContextFlyout>
                        <Flyout>
                            <StackPanel Width="250" Spacing="8" Padding="10">
                                <TextBlock x:Name="SharpenSettingsTitleText" Text="{x:Bind local:Strings.SharpenSettingsTitle}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                                <Grid>
                                    <TextBlock x:Name="UpscaleLabel" Text="{x:Bind local:Strings.UpscaleFactorLabel}" FontSize="12" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="UpscaleValueText" Text="2.0x" FontSize="12" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="UpscaleSlider" Minimum="1.0" Maximum="4.0" StepFrequency="0.1" Value="2.0"/>

                                <Grid>
                                    <TextBlock x:Name="SharpenAmountLabel" Text="{x:Bind local:Strings.SharpenAmountLabel}" FontSize="12" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="SharpenValueText" Text="1.0" FontSize="12" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="SharpenSlider" Minimum="0.0" Maximum="10.0" StepFrequency="0.1" Value="1.0"/>

                                <Grid>
                                    <TextBlock x:Name="SharpenThresholdLabel" Text="{x:Bind local:Strings.SharpenThresholdLabel}" FontSize="12" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="SharpenThresholdValueText" Text="0.010" FontSize="12" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="SharpenThresholdSlider" Minimum="0.0" Maximum="0.3" StepFrequency="0.001" Value="0.010"/>

                                <Grid>
                                    <TextBlock x:Name="UnsharpAmountLabel" Text="{x:Bind local:Strings.UnsharpAmountLabel}" FontSize="12" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="UnsharpAmountValueText" Text="2.0" FontSize="12" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="UnsharpAmountSlider" Minimum="0.0" Maximum="10.0" StepFrequency="0.1" Value="2.0"/>

                                <Grid>
                                    <TextBlock x:Name="UnsharpRadiusLabel" Text="{x:Bind local:Strings.UnsharpRadiusLabel}" FontSize="12" HorizontalAlignment="Left"/>
                                    <TextBlock x:Name="UnsharpRadiusValueText" Text="1.0" FontSize="12" HorizontalAlignment="Right"/>
                                </Grid>
                                <Slider x:Name="UnsharpRadiusSlider" Minimum="0.1" Maximum="10.0" StepFrequency="0.1" Value="1.0"/>

                                <Button x:Name="SharpenParamsResetButton" Content="{x:Bind local:Strings.ResetButton}" Click="SharpenParams_Reset_Click" HorizontalAlignment="Stretch" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Flyout>
                    </ToggleButton.ContextFlyout>
                    <ToggleButton.Resources>
                        <StaticResource x:Key="ToggleButtonForegroundPointerOver" ResourceKey="ToggleButtonForeground" />
                        <StaticResource x:Key="ToggleButtonForegroundCheckedPointerOver" ResourceKey="ToggleButtonForegroundChecked" />
                    </ToggleButton.Resources>
                    <FontIcon x:Name="SharpenIcon" Glyph="S" FontSize="14" FontFamily="Segoe UI" />
                </ToggleButton>

                <AppBarSeparator/>

                <Button x:Name="PrevFileButton" Click="PrevFileButton_Click" ToolTipService.ToolTip="이전 파일">
                    <FontIcon Glyph="&#xE760;" FontSize="14"/>
                </Button>
                <Button x:Name="PrevPageButton" Click="PrevPageButton_Click" ToolTipService.ToolTip="이전 이미지/페이지">
                    <FontIcon Glyph="&#xE76B;" FontSize="14"/>
                </Button>
                <Button x:Name="NextPageButton" Click="NextPageButton_Click" ToolTipService.ToolTip="다음 이미지/페이지">
                    <FontIcon Glyph="&#xE76C;" FontSize="14"/>
                </Button>
                <Button x:Name="NextFileButton" Click="NextFileButton_Click" ToolTipService.ToolTip="다음 파일">
                    <FontIcon Glyph="&#xE761;" FontSize="14"/>
                </Button>

                <Button x:Name="FullscreenButton" Click="FullscreenButton_Click" ToolTipService.ToolTip="전체화면(F11)">
                    <FontIcon x:Name="FullscreenIcon" Glyph="&#xE740;" FontSize="18"/>
                </Button>

                <Button x:Name="CloseWindowButton" Click="CloseWindowButton_Click" ToolTipService.ToolTip="닫기(Esc)">
                    <FontIcon Glyph="&#xE711;" FontSize="18"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Main Content Area -->
        <Grid Grid.Row="2" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SidebarColumn" Width="320"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar Area -->
            <Grid x:Name="SidebarGrid" Grid.Column="0" Background="{ThemeResource LayerFillColorDefaultBrush}" IsTabStop="False"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,1,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Padding="12,8" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                    <Grid.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}" />
                        <Style TargetType="ToggleButton" BasedOn="{StaticResource ToolbarToggleButtonStyle}" />
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" x:Name="ToggleViewButton" Click="ToggleExplorerViewButton_Click"
                            ToolTipService.ToolTip="썸네일 보기" Padding="6" Margin="0,0,8,0">
                        <FontIcon Glyph="&#xE80A;" FontSize="16"/>
                    </Button>

                    <Button Grid.Column="1" x:Name="ParentFolderButton" Click="ParentFolderButton_Click"
                            ToolTipService.ToolTip="상위 폴더로 가기(Backspace)" Padding="6" Margin="0,0,8,0">
                        <FontIcon Glyph="&#xE72B;" FontSize="16"/>
                    </Button>

                    <Button Grid.Column="2" x:Name="SidebarFavoritesButton" Click="SidebarFavoritesButton_Click" 
                            ToolTipService.ToolTip="즐겨찾기" Padding="6" Margin="0,0,8,0">
                        <FontIcon Glyph="&#xE734;" FontSize="16"/>
                        <Button.Flyout>
                            <Flyout x:Name="SidebarFavoritesFlyout" Placement="Bottom">
                                <StackPanel Width="400" MaxHeight="500">
                                    <Button x:Name="SidebarAddToFavoritesButton" Content="➕ 즐겨찾기 추가" Click="AddToFavoritesMenuItem_Click" 
                                            HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                            Background="Transparent" BorderThickness="0"/>
                                    <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,4"/>
                                    <Pivot x:Name="SidebarFavoritesPivot">
                                        <Pivot.HeaderTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" FontSize="14" FontWeight="SemiBold"/>
                                            </DataTemplate>
                                        </Pivot.HeaderTemplate>
                                        <PivotItem x:Name="SidebarFileFavoritesPivotItem" Header="{x:Bind local:Strings.FavoritesFiles}">
                                            <ScrollViewer MaxHeight="400" 
                                                         HorizontalScrollBarVisibility="Disabled" 
                                                         VerticalScrollBarVisibility="Auto">
                                                <uc:BookmarkListControl x:Name="SidebarFileFavoritesList" 
                                                                        ItemClicked="BookmarkList_ItemClicked"
                                                                        RemoveClicked="BookmarkList_RemoveClicked"
                                                                        PinClicked="BookmarkList_PinClicked"/>
                                            </ScrollViewer>
                                        </PivotItem>
                                        <PivotItem x:Name="SidebarFolderFavoritesPivotItem" Header="{x:Bind local:Strings.FavoritesFolders}">
                                            <ScrollViewer MaxHeight="400" 
                                                         HorizontalScrollBarVisibility="Disabled" 
                                                         VerticalScrollBarVisibility="Auto">
                                                <uc:BookmarkListControl x:Name="SidebarFolderFavoritesList" 
                                                                        ItemClicked="BookmarkList_ItemClicked"
                                                                        RemoveClicked="BookmarkList_RemoveClicked"
                                                                        PinClicked="BookmarkList_PinClicked"/>
                                            </ScrollViewer>
                                        </PivotItem>
                                    </Pivot>
                                </StackPanel>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                    
                    <Button Grid.Column="3" x:Name="SidebarRecentButton" Click="SidebarRecentButton_Click" 
                            ToolTipService.ToolTip="최근 이미지" Padding="6" Margin="0,0,8,0">
                        <FontIcon Glyph="&#xE823;" FontSize="16"/>
                        <Button.Flyout>
                            <Flyout x:Name="SidebarRecentFlyout" Placement="Bottom">
                                <ScrollViewer MaxHeight="600" Width="400" 
                                             HorizontalScrollBarVisibility="Disabled" 
                                             VerticalScrollBarVisibility="Auto">
                                        <uc:BookmarkListControl x:Name="SidebarRecentList" 
                                                                ItemClicked="RecentList_ItemClicked"
                                                                RemoveClicked="RecentList_RemoveClicked"/>
                                </ScrollViewer>
                            </Flyout>
                        </Button.Flyout>
                    </Button>

                    <Button Grid.Column="4" x:Name="BrowseFolderButton" Click="BrowseFolderButton_Click" 
                            ToolTipService.ToolTip="폴더 열기" Padding="6" Margin="0,0,8,0">
                        <FontIcon Glyph="&#xED25;" FontSize="16"/>
                    </Button>

                    <Button Grid.Column="5" x:Name="SortByDateButton" Padding="6">
                        <FontIcon x:Name="SortIcon" Glyph="&#xE174;" FontSize="16"/>
                        <Button.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <RadioMenuFlyoutItem x:Name="SortByNameMenu" 
                                                   Click="SortByName_Click" GroupName="SortGroup" IsChecked="True" />
                                <RadioMenuFlyoutItem x:Name="SortByDateDescMenu" 
                                                   Click="SortByDateDesc_Click" GroupName="SortGroup" />
                                <RadioMenuFlyoutItem x:Name="SortByDateAscMenu" 
                                                   Click="SortByDateAsc_Click" GroupName="SortGroup" />
                            </MenuFlyout>
                        </Button.Flyout>
                    </Button>

                    <Button Grid.Column="7" x:Name="WebDavButton"
                            ToolTipService.ToolTip="webdav 연결" Padding="6"
                            HorizontalAlignment="Right">
                        <FontIcon Glyph="&#xE774;" FontSize="16"/>
                        <Button.Flyout>
                            <Flyout x:Name="WebDavFlyout" Placement="Bottom" Opened="WebDavFlyout_Opened">
                                <ScrollViewer MaxHeight="400" Width="300"
                                             HorizontalScrollBarVisibility="Disabled"
                                             VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="WebDavPanel" Spacing="2">
                                        <Button x:Name="AddWebDavButton" Content="➕ webdav 서버 추가" Click="AddWebDavButton_Click"
                                                HorizontalAlignment="Stretch" HorizontalContentAlignment="Left"
                                                Background="Transparent" BorderThickness="0"/>
                                        <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,4"/>
                                        <!-- Saved WebDAV servers will be added dynamically -->
                                    </StackPanel>
                                </ScrollViewer>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                </Grid>

                <!-- Current Path -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="CurrentPathText" Grid.Row="0" 
                               Text="폴더를 선택하세요" 
                               Padding="12,8" FontSize="11"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextTrimming="CharacterEllipsis"/>

                    <!-- File List -->
                    <ListView x:Name="FileListView" Grid.Row="1" 
                              SelectionMode="Single"
                              SelectionChanged="FileListView_SelectionChanged"
                              IsItemClickEnabled="True"
                              ItemClick="FileItem_ItemClick"
                              PreviewKeyDown="FileListView_PreviewKeyDown">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="4,6" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <FontIcon Grid.Column="0" Glyph="{Binding Icon}" FontSize="14"
                                              Foreground="{Binding IconColor}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                               VerticalAlignment="Center"
                                               FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTipService.ToolTip="{Binding Name}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <GridView x:Name="FileGridView" Grid.Row="1"
                              SelectionMode="Single"
                              SelectionChanged="FileGridView_SelectionChanged"
                              PreviewKeyDown="FileGridView_PreviewKeyDown"
                              IsItemClickEnabled="True"
                              ItemClick="FileItem_ItemClick"
                              Visibility="Collapsed"
                              Padding="4">
                        <GridView.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="100" Height="120" Padding="4" Background="Transparent" CornerRadius="4">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Grid Grid.Row="0">
                                        <FontIcon Glyph="{Binding Icon}" FontSize="48" Foreground="{Binding IconColor}" 
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>

                                    <TextBlock Grid.Row="1" Text="{Binding Name}" FontSize="11" 
                                               FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                               TextTrimming="CharacterEllipsis" HorizontalAlignment="Center" 
                                               MaxLines="2" TextAlignment="Center" Margin="0,4,0,0"/>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>
                </Grid>
            </Grid>

            <controls:GridSplitter 
                x:Name="SplitterGrid"
                Grid.Column="1"
                Width="8"
                Background="Transparent"
                Foreground="{ThemeResource CardStrokeColorDefaultBrush}"
                ResizeBehavior="BasedOnAlignment"
                ResizeDirection="Columns" />

            <!-- Image Area with Win2D CanvasControl -->
            <Grid Grid.Column="2" x:Name="ImageArea" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                  IsTabStop="False"
                  SizeChanged="ImageArea_SizeChanged"
                  PointerWheelChanged="ImageArea_PointerWheelChanged"
                  PointerPressed="ImageArea_PointerPressed"
                  ManipulationMode="Scale, TranslateX, TranslateY"
                  ManipulationDelta="ImageArea_ManipulationDelta"
                  ManipulationCompleted="ImageArea_ManipulationCompleted">
                <!-- Empty State -->
                <StackPanel x:Name="EmptyStatePanel" HorizontalAlignment="Center" VerticalAlignment="Center" 
                            Spacing="16" Visibility="Visible">
                    <FontIcon Glyph="&#xE8B9;" FontSize="64" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="이미지를 여기에 드래그하거나" 
                               FontSize="18" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="'열기' 버튼을 클릭하세요" 
                               FontSize="14" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                               HorizontalAlignment="Center"/>
                    <Button Content="이미지 열기" Click="OpenFileButton_Click" 
                            Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                </StackPanel>

                <Grid x:Name="FastNavOverlay"
								Canvas.ZIndex="999"
								IsHitTestVisible="False"
								Visibility="Collapsed"
								Background="#80000000"
								HorizontalAlignment="Stretch"
								VerticalAlignment="Stretch">
                    <TextBlock x:Name="FastNavText"
											Foreground="White"
											FontSize="28"
											FontWeight="SemiBold"
											HorizontalAlignment="Center"
											VerticalAlignment="Center"
											Text="빠른 탐색 중... (1/1)" />
                </Grid>

                <!-- Single Image Display with Win2D -->
                <canvas:CanvasControl x:Name="MainCanvas" 
                                     Draw="MainCanvas_Draw"
                                     Visibility="Collapsed"
                                     IsTabStop="False"
                                     CreateResources="MainCanvas_CreateResources"/>

                <!-- Side-by-Side Image Display with Win2D -->
                <Grid x:Name="SideBySideGrid" Visibility="Collapsed" ColumnSpacing="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <canvas:CanvasControl x:Name="LeftCanvas" 
                                         Grid.Column="0"
                                         IsTabStop="False"
                                         Draw="LeftCanvas_Draw"
                                         CreateResources="LeftCanvas_CreateResources"/>

                    <canvas:CanvasControl x:Name="RightCanvas" 
                                         Grid.Column="1"
                                         IsTabStop="False"
                                         Draw="RightCanvas_Draw"
                                         CreateResources="RightCanvas_CreateResources"/>
                </Grid>
            </Grid>

            <!-- Text Display Area -->
            <Grid Grid.Column="2" x:Name="TextArea" Visibility="Collapsed" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                  IsTabStop="False"
                  PointerPressed="TextArea_PointerPressed" PointerWheelChanged="TextArea_PointerWheelChanged"
                  SizeChanged="TextArea_SizeChanged">
                 <ScrollViewer x:Name="TextScrollViewer" 
                               Margin="0,0,0,10"
                               Padding="20,10,20,10"
                               HorizontalScrollBarVisibility="Disabled"
                               VerticalScrollBarVisibility="Hidden"
                               SizeChanged="TextScrollViewer_SizeChanged"
                               ViewChanged="TextScrollViewer_ViewChanged">
                     <ItemsRepeater x:Name="TextItemsRepeater" ElementPrepared="TextItemsRepeater_ElementPrepared" 
                                    ItemTemplate="{StaticResource TextItemTemplate}"
                                    Margin="0,0,0,40">
                         <ItemsRepeater.Layout>
                             <StackLayout Spacing="0"/>
                         </ItemsRepeater.Layout>
                     </ItemsRepeater>
                 </ScrollViewer>
                 
                 <!-- Win2D Canvas for Aozora rendering (replaces XAML-based container) -->
                 <canvas:CanvasControl x:Name="AozoraTextCanvas"
                                       Visibility="Collapsed"
                                       IsTabStop="False"
                                       Draw="AozoraTextCanvas_Draw"
                                       CreateResources="AozoraTextCanvas_CreateResources"
                                       PointerPressed="AozoraTextCanvas_PointerPressed"
                                       PointerWheelChanged="AozoraTextCanvas_PointerWheelChanged"
                                       SizeChanged="AozoraTextCanvas_SizeChanged"/>
                 
                  <Grid x:Name="TextFastNavOverlay"
                        Canvas.ZIndex="999"
                        IsHitTestVisible="False"
                        Visibility="Collapsed"
                        Background="#80000000"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch">
                    <TextBlock x:Name="TextFastNavText"
                            Foreground="White"
                            FontSize="28"
                            FontWeight="SemiBold"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="페이지 이동 중..." />
                </Grid>

                <!-- Vertical Text Display (Win2D) -->
                <canvas:CanvasControl x:Name="VerticalTextCanvas"
                                     Visibility="Collapsed"
                                     IsTabStop="False"
                                     Draw="VerticalTextCanvas_Draw"
                                     CreateResources="VerticalTextCanvas_CreateResources"
                                     PointerPressed="VerticalTextCanvas_PointerPressed"
                                     PointerWheelChanged="VerticalTextCanvas_PointerWheelChanged"
                                     SizeChanged="VerticalTextCanvas_SizeChanged"/>
            </Grid>
            
            <!-- Epub Display Area -->
            <Grid Grid.Column="2" x:Name="EpubArea" Visibility="Collapsed" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                  IsTabStop="False"
                  SizeChanged="EpubArea_SizeChanged">
                <!-- Win2D Canvas for EPUB text rendering (DirectWrite) -->
                <canvas:CanvasControl x:Name="EpubTextCanvas"
                                      Visibility="Collapsed"
                                      IsTabStop="True"
                                      AllowFocusOnInteraction="True"
                                      Draw="EpubTextCanvas_Draw"
                                      CreateResources="EpubTextCanvas_CreateResources"
                                      SizeChanged="EpubTextCanvas_SizeChanged"/>
                <!-- Image host for EPUB image pages -->
                <Grid x:Name="EpubImageHost" Visibility="Collapsed"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      ColumnSpacing="0" Padding="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="EpubImageLeftColumn" Width="*"/>
                        <ColumnDefinition x:Name="EpubImageRightColumn" Width="0"/>
                    </Grid.ColumnDefinitions>
                    <canvas:CanvasControl x:Name="EpubCanvasDisplay" Grid.Column="0" Grid.ColumnSpan="2"
                                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                          Draw="EpubCanvasDisplay_Draw"/>
                    <canvas:CanvasControl x:Name="EpubCanvasDisplayLeft" Grid.Column="0" Visibility="Collapsed"
                                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                          Draw="EpubCanvasDisplayLeft_Draw"/>
                    <canvas:CanvasControl x:Name="EpubCanvasDisplayRight" Grid.Column="1" Visibility="Collapsed"
                                          HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                          Draw="EpubCanvasDisplayRight_Draw"/>
                </Grid>
                <!-- Touch Overlay for Page Navigation (left half = prev, right half = next) -->
                <Grid x:Name="EpubTouchOverlay" 
                      Background="Transparent" 
                      AllowFocusOnInteraction="True"
                      IsTabStop="True"
                      PointerPressed="EpubTouchOverlay_PointerPressed"
                      PointerWheelChanged="EpubTouchOverlay_PointerWheelChanged"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"/>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Grid x:Name="StatusBarGrid" Grid.Row="3" Height="28" Padding="12,0" 
              Canvas.ZIndex="10"
              Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0"
              IsTabStop="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="FileNameText" Grid.Column="0" 
                       Text="파일을 선택해주세요" 
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"/>

            <TextBlock x:Name="ImageInfoText" Grid.Column="1" 
                       Text="" 
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextAlignment="Right"
                       Margin="0,0,16,0"/>

            <TextBlock x:Name="TextProgressText" Grid.Column="2" 
                       Text="" 
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextAlignment="Right"
                       Margin="0,0,16,0"/>

            <TextBlock x:Name="ImageIndexText" Grid.Column="3" 
                       Text="" 
                       FontSize="12" 
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       VerticalAlignment="Center"
                       TextAlignment="Right"/>
        </Grid>

        <!-- Notification Overlay -->
        <Border x:Name="NotificationOverlay" 
                Grid.RowSpan="4"
                HorizontalAlignment="Center" 
                VerticalAlignment="Bottom"
                Margin="0,0,0,100"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                Padding="24,12"
                CornerRadius="20"
                BorderThickness="1"
                BorderBrush="{ThemeResource SystemControlForegroundChromeHighBrush}"
                Visibility="Collapsed"
                Canvas.ZIndex="1000">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>
            <StackPanel Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE735;" Foreground="Gold" FontSize="20"/>
                <TextBlock x:Name="NotificationText" Text="즐겨찾기에 추가되었습니다" 
                           VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
        </Grid>
    </ContentControl>
</Window>
