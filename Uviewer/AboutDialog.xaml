<ContentDialog
    x:Class="Uviewer.AboutDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Uviewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="{x:Bind local:Strings.AboutTitle}">

    <StackPanel Spacing="12" Width="320" HorizontalAlignment="Center">
        <Image Source="ms-appx:///Assets/Uviewer2.png" 
               Width="100" Height="100" 
               Margin="0,10,0,10" 
               HorizontalAlignment="Center"/>

        <TextBlock Text="{x:Bind VersionText, Mode=OneWay}" 
                   FontSize="24" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,8" 
                   TextAlignment="Center"/>

        <HyperlinkButton Content="Project GitHub" 
                         NavigateUri="https://github.com/kirinonakar/Uviewer" 
                         HorizontalAlignment="Center" 
                         Margin="0,0,0,10"/>

        <Button Content="{x:Bind local:Strings.Close}"
                HorizontalAlignment="Center"
                Margin="0,10,0,0"
                Width="100"
                Click="CloseButton_Click"/>
    </StackPanel>
</ContentDialog>
